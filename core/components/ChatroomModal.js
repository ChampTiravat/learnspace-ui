import React from "react";
import styled from "styled-components";

import Modal from "./Modal";
import { Header, Body } from "./Card";

const ChatroomModal = () => (
  <Modal large>
    <Header>แชทรวม</Header>
    <Body>asdsd</Body>
  </Modal>
);

export default ChatroomModal;
